@model IEnumerable<WebApplicationAchats.Models.Clientadress>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Address1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.State)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumcliNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Address1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.State)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumcliNavigation.Numcli)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Numcli">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Numcli">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Numcli">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
